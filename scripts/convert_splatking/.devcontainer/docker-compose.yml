# Copyright Contributors to the OpenVDB Project
# SPDX-License-Identifier: Apache-2.0
#
# Docker Compose for the SplatKing-to-COLMAP devcontainer.
# Referenced by devcontainer.json -- not typically invoked directly.
#
# To mount your SplatKing data directory, set DATA_DIR before opening the container:
#   export DATA_DIR=/path/to/splatking/captures

services:
  devcontainer:
    build:
      context: ..
      dockerfile: .devcontainer/Dockerfile
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
    volumes:
      - ..:/workspace:cached
      - ${DATA_DIR:-/data}:/data
    command: sleep infinity
